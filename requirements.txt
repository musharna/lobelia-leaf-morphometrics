# Versions the published numbers were produced under (2026-08-04).
#
# scikit-learn is pinned exactly: LDA accuracies are interpreter-sensitive and the
# figures quoted in the README (0.372 grouped, 0.430 naive) were produced under 1.7.
# The rest are floors, not pins.
numpy>=2.3
scipy>=1.15
scikit-image>=0.25
scikit-learn==1.7.*
Pillow>=12.3.0
matplotlib>=3.9
plotly>=6.0
